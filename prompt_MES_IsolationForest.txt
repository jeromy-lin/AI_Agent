你是一位熟悉製造業 MES、Python、scikit-learn、
Isolation Forest、異常偵測與 Google Colab 的資料分析工程師。

請產生一份可以直接在 Google Colab CPU 環境執行的完整 Python 程式碼。

本次任務使用：

YUNFA_MES.xlsx

建立：

YUNFA MES－Isolation Forest 多來源、多變數製程異常偵測


============================================================
最重要規則｜必須精確重現既定 Golden Version
============================================================

本次不是重新設計 Isolation Forest 方法。

請依照既定的：

YUNFA_MES_IsolationForest.py

資料處理、Feature Engineering、Historical Context、
StandardScaler、Isolation Forest、Rule-Based、
Anomaly Score、PCA 與結果呈現方式進行。

不可自行：

- 新增 Feature
- 刪除 Feature
- 改變 Feature 順序
- 更換 Feature 公式
- 改變歷史年度
- 改變偵測年度
- 修改 Isolation Forest 參數
- 修改 Rule-Based 指標
- 修改 Rule Threshold 計算方式
- 修改 Context Baseline 群組
- 修改 Missing Value 處理方式
- 修改 Anomaly Score 計算方式
- 對資料額外 drop_duplicates
- 對模型資料額外 dropna
- 在模型建立前自行 sort_values
- 改變 Merge Key
- 將 2026 設備日資料放入模型

分析單位固定為：

工單 × 工序 × 機台 × 一次實際生產報工


============================================================
STEP 0｜Import Packages & Parameters
============================================================

使用：

import io
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

from IPython.display import display
from google.colab import files

from sklearn.ensemble import IsolationForest
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA


固定參數：

HISTORY_YEARS = [2023, 2024, 2025]

DETECTION_YEAR = 2026

CONTAMINATION = 0.05

RANDOM_STATE = 42

TOP_N = 15


固定配色：

TEXT_DARK = "#3F3F3F"

PALETTE = {
    "orange": "#F2C6A0",
    "green": "#A8C69F",
    "blue": "#AFC8E6",
    "purple": "#D7C4E8",
    "red": "#D9534F",
    "gold": "#E8D58A",
    "mint": "#B7D7D0",
    "gray": "#D9D9D9",
}


============================================================
STEP 1｜Upload YUNFA MES Excel
============================================================

使用：

uploaded = files.upload()

取得：

file_name = next(iter(uploaded))

excel_bytes = io.BytesIO(uploaded[file_name])

顯示：

已載入檔案：{file_name}

不可使用固定本機路徑。


============================================================
STEP 2｜Read MES Sheets
============================================================

使用：

xls = pd.ExcelFile(excel_bytes)

固定檢查以下 6 張 Sheet：

機台主檔
工藝路由主檔
生產工單
生產報工
機台稼動日報
品質異常


DataFrame 名稱固定：

machine_df = pd.read_excel(xls, sheet_name="機台主檔")

route_df = pd.read_excel(xls, sheet_name="工藝路由主檔")

wo_df = pd.read_excel(xls, sheet_name="生產工單")

report_df = pd.read_excel(xls, sheet_name="生產報工")

equip_df = pd.read_excel(xls, sheet_name="機台稼動日報")

quality_abn_df = pd.read_excel(xls, sheet_name="品質異常")


若缺少必要 Sheet：

raise ValueError

並顯示：

找不到必要 Sheet

以及目前 Excel Sheet 名稱。


每張 Sheet：

完整讀取。

畫面只使用：

df.head()

顯示前 5 筆。

請顯示：

「程式已讀取整張工作表；以下僅顯示前 5 筆供資料檢視。」


使用 Pandas Styler：

白底
柔和暖色
表格框線
適當 padding


============================================================
STEP 3｜Data Cleaning
============================================================

建立：

to_num(df, cols)


使用：

pd.to_numeric(..., errors="coerce")


日期欄位固定如下。


【生產報工】

計畫開始時間
計畫結束時間
實際開始時間
實際結束時間

使用：

pd.to_datetime(..., errors="coerce")


【生產工單】

開單日期
計畫開工日期
計畫完工日期
交期
實際開工日期
實際完工日期


【機台稼動日報】

日期

固定：

pd.to_datetime(..., errors="coerce").dt.normalize()


【工藝路由主檔】

生效日期


【品質異常】

發現日期


數值欄位固定如下。


【生產報工】

工序序號
標準換線工時_hr
實際換線工時_hr
標準加工工時_hr
實際加工工時_hr
停機工時_hr
良品數量
報廢數量
重工數量
不良率


【機台主檔】

每日可用工時
效率目標
設備年齡_年


【機台稼動日報】

可用工時_hr
運轉工時_hr
換線工時_hr
停機工時_hr
稼動率
品質良率
OEE
故障次數


============================================================
STEP 4｜Build Event-Level MES Dataset
============================================================

建立：

events = report_df.copy()


分析單位固定：

工單 × 工序 × 機台 × 一次實際生產報工


============================================================
STEP 4-1｜Merge 生產工單
============================================================

wo_keep 固定為：

[
    "工單ID",
    "產品線",
    "開單日期",
    "計畫開工日期",
    "計畫完工日期",
    "交期",
    "計畫數量",
    "急單",
    "優先等級",
    "材料齊套",
    "品質風險",
    "狀態",
]


固定：

events = events.merge(
    wo_df[wo_keep],
    on="工單ID",
    how="left"
)


年度切分唯一依據：

開單日期


建立：

events["工單年度"] =
events["開單日期"].dt.year


不可使用：

報工年份
實際開始年份
實際完工年份
計畫完工年份


============================================================
STEP 4-2｜建立報工日期
============================================================

報工日期唯一來源：

實際開始時間


固定：

events["報工日期"] =
events["實際開始時間"].dt.normalize()


不可直接假設「報工日期」已存在。


============================================================
STEP 4-3｜Merge 機台主檔
============================================================

machine_keep 固定為：

[
    "機台ID",
    "機台類型",
    "工作中心",
    "能力群組",
    "每日可用工時",
    "效率目標",
    "設備年齡_年",
    "狀態",
]


建立：

machine_merge =
machine_df[machine_keep].copy()


機台主檔中的：

工作中心

固定改名：

機台工作中心


固定：

events = events.merge(
    machine_merge,
    on="機台ID",
    how="left"
)


非常重要：

生產報工原本的：

工作中心

必須保留。

後續 Context Baseline 的 fallback 使用：

events["工作中心"]

不是：

events["機台工作中心"]


============================================================
STEP 4-4｜Merge 機台稼動日報
============================================================

equip_keep 固定為：

[
    "日期",
    "機台ID",
    "可用工時_hr",
    "運轉工時_hr",
    "換線工時_hr",
    "停機工時_hr",
    "稼動率",
    "品質良率",
    "OEE",
    "故障次數",
]


建立：

equip_merge = equip_df[equip_keep]


固定重新命名：

日期
→ 報工日期

停機工時_hr
→ 設備日停機工時_hr


固定：

events = events.merge(
    equip_merge,
    on=["報工日期", "機台ID"],
    how="left"
)


非常重要：

不要額外：

drop_duplicates()

不要額外：

groupby()

不要額外：

sort_values()


請完全依照原始 Golden Version Merge。


同時注意：

生產報工的：

停機工時_hr

必須保留。


設備日的：

設備日停機工時_hr

是另一個不同欄位。


============================================================
STEP 4-5｜Merge 品質異常
============================================================

固定以：

["工單ID", "機台ID"]

GroupBy。


建立：

品質異常單數 =
異常單ID count


品質異常影響數量 =
受影響數量 sum


再：

how="left"

Merge 回 events。


最後：

品質異常單數 fillna(0)

品質異常影響數量 fillna(0)


品質異常：

不可進入 Isolation Forest Feature。

只作 Post-hoc Validation。


============================================================
STEP 5｜Raw Manufacturing Indicators
============================================================

建立：

safe_divide(a, b)


固定：

a = pd.to_numeric(a, errors="coerce")

b = pd.to_numeric(b, errors="coerce")

return a / b.replace(0, np.nan)


以下公式必須完全一致。


============================================================
指標 1｜換線超時率
============================================================

公式：

換線超時率 =

(實際換線工時_hr - 標準換線工時_hr)
÷
標準換線工時_hr


用途：

Isolation Forest = YES

Rule-Based = YES

Context Source = YES


Context 對應：

換線情境偏差_Z


Rule：

HIGH
Historical 95th percentile


English：

Setup Overrun


============================================================
指標 2｜加工超時率
============================================================

公式：

加工超時率 =

(實際加工工時_hr - 標準加工工時_hr)
÷
標準加工工時_hr


用途：

Isolation Forest = YES

Rule-Based = YES

Context Source = YES


Context：

加工情境偏差_Z


Rule：

HIGH
Historical 95th percentile


English：

Processing Overrun


============================================================
指標 3｜停機占比
============================================================

公式：

停機占比 =

生產報工["停機工時_hr"]
÷
機台主檔["每日可用工時"]


非常重要：

分子不可使用：

設備日停機工時_hr


分母不可使用：

可用工時_hr


正確來源只能是：

生產報工停機工時_hr
÷
機台主檔每日可用工時


用途：

Isolation Forest = YES

Rule-Based = YES

Context Source = YES


Context：

停機情境偏差_Z


Rule：

HIGH
Historical 95th percentile


English：

Downtime Ratio


============================================================
指標 4｜總產出數量
============================================================

公式：

總產出數量 =

良品數量.fillna(0)
+
報廢數量.fillna(0)
+
重工數量.fillna(0)


此變數：

不是 Isolation Forest Feature。

不是 Rule-Based Indicator。

只用於計算：

報廢率
重工率


============================================================
指標 5｜報廢率
============================================================

公式：

報廢率 =

報廢數量
÷
總產出數量


最後：

fillna(0)


用途：

Isolation Forest = YES

Rule-Based = YES

Context Source = YES


Context：

報廢情境偏差_Z


Rule：

HIGH
Historical 95th percentile


English：

Scrap Rate


============================================================
指標 6｜重工率
============================================================

公式：

重工率 =

重工數量
÷
總產出數量


最後：

fillna(0)


用途：

Isolation Forest = YES

Rule-Based = YES

Context Source = YES


Context：

重工情境偏差_Z


Rule：

HIGH
Historical 95th percentile


English：

Rework Rate


============================================================
指標 7｜良品產出率_件每時
============================================================

公式：

良品產出率_件每時 =

良品數量
÷
實際加工工時_hr


此欄位：

不是 Isolation Forest 最終 Feature。

不是 Rule-Based 直接指標。


它唯一用於建立：

產出效率偏差_Z


也就是：

良品產出率_件每時
→ 產出效率偏差_Z


============================================================
STEP 6｜Historical Context Baseline
============================================================

固定：

history_mask =
events["工單年度"].isin(
    [2023, 2024, 2025]
)


detect_mask =
events["工單年度"].eq(2026)


若歷史資料為 0：

raise ValueError


若 2026 資料為 0：

列出 available_years

再 raise ValueError


============================================================
STEP 6-1｜add_context_z() 固定演算法
============================================================

函式參數：

full_df
history_df
value_col
primary_group
fallback_group
output_col


歷史 Global：

global_mean =
history_df[value_col].mean()


global_std =
history_df[value_col].std()


Primary Group 固定：

["SKU", "工序序號"]


Fallback Group 固定：

["工作中心"]


先建立 Primary：

groupby(primary_group)[value_col]
.agg(["mean", "std"])


建立：

_p_mean
_p_std


再建立 Fallback：

groupby(fallback_group)[value_col]
.agg(["mean", "std"])


建立：

_f_mean
_f_std


首先使用：

_p_mean
_p_std


只有以下任一成立時才 fallback：

mean_ref.isna()

std_ref.isna()

abs(std_ref) < 1e-9


Fallback 到：

_f_mean
_f_std


如果 fallback 後仍發生：

mean_ref.isna()

std_ref.isna()

abs(std_ref) < 1e-9


才使用：

global_mean
global_std


若：

global_std 為 NaN

或：

abs(global_std) < 1e-9


固定：

global_std = 1.0


Context Z：

(目前值 - mean_ref)
÷
std_ref


最後：

replace([np.inf, -np.inf], np.nan)

fillna(0)


============================================================
STEP 6-2｜六組 Context Mapping
============================================================

必須一對一固定：


換線超時率
→ 換線情境偏差_Z


加工超時率
→ 加工情境偏差_Z


停機占比
→ 停機情境偏差_Z


報廢率
→ 報廢情境偏差_Z


重工率
→ 重工情境偏差_Z


良品產出率_件每時
→ 產出效率偏差_Z


不得交換來源。


============================================================
STEP 7｜Exact Isolation Forest FEATURES
============================================================

FEATURES 必須完全固定，
包含順序也不可改：

FEATURES = [
    "換線超時率",
    "加工超時率",
    "停機占比",
    "報廢率",
    "重工率",
    "換線情境偏差_Z",
    "加工情境偏差_Z",
    "停機情境偏差_Z",
    "報廢情境偏差_Z",
    "重工情境偏差_Z",
    "產出效率偏差_Z",
]


結構為：

5 個直接製程指標
+
6 個 Context-Aware 指標
=
11 個 Features


注意：

良品產出率_件每時

不直接進入 FEATURES。


============================================================
STEP 7-1｜Exact English Mapping
============================================================

FEATURE_EN 固定：


換線超時率
→ Setup Overrun


加工超時率
→ Processing Overrun


停機占比
→ Downtime Ratio


報廢率
→ Scrap Rate


重工率
→ Rework Rate


換線情境偏差_Z
→ Context Setup Deviation


加工情境偏差_Z
→ Context Processing Deviation


停機情境偏差_Z
→ Context Downtime Deviation


報廢情境偏差_Z
→ Context Scrap Deviation


重工情境偏差_Z
→ Context Rework Deviation


產出效率偏差_Z
→ Throughput Deviation


============================================================
STEP 7-2｜Feature Description Table
============================================================

建立：

feature_desc


欄位固定：

變數
商業意義


商業意義固定：

換線超時率
→ 實際換線時間相對標準值的超出比例

加工超時率
→ 實際加工時間相對標準值的超出比例

停機占比
→ 停機工時占機台每日可用工時比例

報廢率
→ 報廢數量占總產出比例

重工率
→ 重工數量占總產出比例

換線情境偏差_Z
→ 換線表現相對同 SKU / 工序歷史基準的偏差

加工情境偏差_Z
→ 加工表現相對同 SKU / 工序歷史基準的偏差

停機情境偏差_Z
→ 停機表現相對同 SKU / 工序歷史基準的偏差

報廢情境偏差_Z
→ 報廢表現相對同 SKU / 工序歷史基準的偏差

重工情境偏差_Z
→ 重工表現相對同 SKU / 工序歷史基準的偏差

產出效率偏差_Z
→ 每小時良品產出相對同 SKU / 工序歷史基準的偏差


============================================================
STEP 7-3｜Missing Value Protection
============================================================

固定：

events[FEATURES] = (
    events[FEATURES]
    .replace([np.inf, -np.inf], np.nan)
)


只使用 2023–2025 建立：

history_medians = (
    events.loc[history_mask, FEATURES]
    .median(numeric_only=True)
)


再固定：

events[FEATURES] = (
    events[FEATURES]
    .fillna(history_medians)
    .fillna(0)
)


非常重要：

不可使用 2026 Median。

不可先直接 fillna(0) 而跳過 historical median。

不可用 SimpleImputer 取代。

不可 dropna。

不可刪除事件。


============================================================
STEP 8｜Timeline Split
============================================================

固定：

train_df =
events.loc[history_mask]
.copy()
.reset_index(drop=True)


detect_df =
events.loc[detect_mask]
.copy()
.reset_index(drop=True)


X_train =
train_df[FEATURES].copy()


X_detect =
detect_df[FEATURES].copy()


不可使用：

train_test_split

不可 Random Split。


============================================================
STEP 9｜Standardization
============================================================

固定：

scaler = StandardScaler()


X_train_scaled =
scaler.fit_transform(X_train)


X_detect_scaled =
scaler.transform(X_detect)


非常重要：

StandardScaler 只能 fit 2023–2025。


2026：

只能 transform。


不要在 PCA 前重新建立另一個 scaler。


不要重新 fit。


============================================================
STEP 10｜Isolation Forest
============================================================

固定：

iso = IsolationForest(
    n_estimators=400,
    max_samples="auto",
    contamination=0.05,
    random_state=42,
    n_jobs=-1
)


固定：

iso.fit(X_train_scaled)


建立：

train_score_raw =
-iso.score_samples(X_train_scaled)


detect_score_raw =
-iso.score_samples(X_detect_scaled)


建立：

detect_df["IF判定"]


判定固定：

iso.predict(X_detect_scaled) == -1
→ 異常


其他：
→ 正常


============================================================
STEP 10-1｜Anomaly Score
============================================================

Anomaly Score 必須完全使用歷史 Percentile。


固定：

sorted_train_scores =
np.sort(train_score_raw)


2026 每筆：

100.0
*
np.searchsorted(
    sorted_train_scores,
    score,
    side="right"
)
/
len(sorted_train_scores)


不得使用：

MinMaxScaler

decision_function percentile

rank percentage

2026 自身 percentile


============================================================
STEP 10-2｜Anomaly Rank
============================================================

固定：

detect_df["Anomaly Rank"] = (
    detect_df["Anomaly Score"]
    .rank(
        method="first",
        ascending=False
    )
    .astype(int)
)


============================================================
STEP 10-3｜異常層級
============================================================

固定：

IF判定 == "異常"
→ High Anomaly


否則：

Anomaly Score >= 95
→ Review


其他：

Normal


注意：

Review 不等於 IF 異常。


只有：

IF判定 == "異常"

才是真正 Isolation Forest 異常。


============================================================
STEP 11｜Exact Rule-Based Baseline
============================================================

Rule-Based 只使用 6 個指標。

不可把 11 個 Features 全部做 Rule。


固定：

rule_specs = {

    "換線超時率":
    ("high", 0.95),

    "加工超時率":
    ("high", 0.95),

    "停機占比":
    ("high", 0.95),

    "報廢率":
    ("high", 0.95),

    "重工率":
    ("high", 0.95),

    "產出效率偏差_Z":
    ("low", 0.05),

}


============================================================
Rule 1｜換線超時率
============================================================

Threshold：

train_df["換線超時率"].quantile(0.95)

判定：

2026 value > threshold

才 Flag。


不是 >=。


============================================================
Rule 2｜加工超時率
============================================================

Threshold：

train_df["加工超時率"].quantile(0.95)

判定：

2026 value > threshold


============================================================
Rule 3｜停機占比
============================================================

Threshold：

train_df["停機占比"].quantile(0.95)

判定：

2026 value > threshold


============================================================
Rule 4｜報廢率
============================================================

Threshold：

train_df["報廢率"].quantile(0.95)

判定：

2026 value > threshold


============================================================
Rule 5｜重工率
============================================================

Threshold：

train_df["重工率"].quantile(0.95)

判定：

2026 value > threshold


============================================================
Rule 6｜產出效率偏差_Z
============================================================

Threshold：

train_df["產出效率偏差_Z"].quantile(0.05)

方向：

LOW


判定：

2026 value < threshold

才 Flag。


不是 <=。


============================================================
STEP 11-1｜Rule Flag Count
============================================================

建立：

rule_flags = pd.DataFrame(
    index=detect_df.index
)


六個 Rule Boolean 分別建立。


固定：

detect_df["Rule Flag Count"] =
rule_flags.sum(axis=1)


固定：

Rule Flag Count >= 1
→ Warning


Rule Flag Count == 0
→ Normal


============================================================
STEP 11-2｜方法對照
============================================================

建立：

detect_df["方法對照"]


固定四種：


Rule Warning
+
IF 異常

→ 共同警示


Rule Normal
+
IF 異常

→ 多變數隱性異常


Rule Warning
+
IF 正常

→ 單變數規則警示


Rule Normal
+
IF 正常

→ 共同正常


必須使用：

Rule判定

與：

IF判定


不可用：

Anomaly Score >= 95

取代 IF判定。


============================================================
STEP 12｜2026 Equipment Context
============================================================

以下為設備後續情境：

OEE
稼動率
品質良率
故障次數


它們：

不進 Isolation Forest

不進 Rule-Based

不進 StandardScaler

不進 Historical Context


只作 2026 結果解讀。


建立：

設備日資料可用


若：

OEE.notna()

→ 是

否則：

→ 否


============================================================
STEP 13｜Quality Post-hoc Validation
============================================================

品質異常不參與模型。


建立：

有品質異常紀錄


若：

品質異常單數 > 0

→ 是


否則：

→ 否


計算：

IF判定 == "異常"

事件中：

有品質異常紀錄 == "是"

的比例。


============================================================
STEP 14｜Model Summary
============================================================

顯示：

YUNFA MES－Isolation Forest 多來源製程異常偵測


內容：

歷史基準年度
偵測年度
歷史基準事件數
2026 偵測事件數
Isolation Forest contamination
模型判定異常事件數
多變數隱性異常事件數
異常事件具有品質異常紀錄比例


多變數隱性異常事件數固定由：

detect_df["方法對照"]
.eq("多變數隱性異常")
.sum()

計算。


============================================================
STEP 15｜Top 15 MES Anomaly Table
============================================================

固定排序：

detect_df.sort_values(
    ["Anomaly Score", "Rule Flag Count"],
    ascending=[False, False]
)


取：

head(15)


表格固定顯示：

報工ID
工單ID
SKU
工序序號
工序名稱
工作中心
機台ID

Anomaly Score
Anomaly Rank
異常層級

Rule Flag Count
Rule判定
IF判定
方法對照

換線超時率
加工超時率
停機占比
報廢率
重工率

OEE
稼動率
品質良率
故障次數

有品質異常紀錄


============================================================
STEP 15-1｜Top 15 Formatting
============================================================

Anomaly Score：

{:.1f}


換線超時率：

{:.1%}


加工超時率：

{:.1%}


停機占比：

{:.1%}


報廢率：

{:.2%}


重工率：

{:.2%}


OEE：

{:.2%}


稼動率：

{:.2%}


品質良率：

{:.2%}


異常層級：

High Anomaly
→ 淡紅色


Review
→ 淡黃色


Normal
→ 淡綠色


方法對照：

共同警示
→ 淡紅色 + 粗體


多變數隱性異常
→ 淡紫色 + 粗體


單變數規則警示
→ 淡黃色


共同正常
→ 淡綠色


不要另外建立一張
「多變數隱性異常表」。

直接在：

方法對照

欄位中標註即可。


============================================================
STEP 16｜Rule-Based vs Isolation Forest
============================================================

固定：

comparison_table = pd.crosstab(
    detect_df["Rule判定"],
    detect_df["IF判定"]
).reindex(
    index=["Normal", "Warning"],
    columns=["正常", "異常"],
    fill_value=0
)


四格意義固定：


Normal / 正常
→ 共同正常


Normal / 異常
→ 多變數隱性異常


Warning / 正常
→ 單變數規則警示


Warning / 異常
→ 共同警示


============================================================
STEP 17｜Top Anomaly Multi-Factor Profile
============================================================

固定：

top_row =
top_anomaly.iloc[0]


取得：

top_pos =
top_anomaly.index[0]


取得：

detect_position =
detect_df.index.get_loc(top_pos)


取得：

top_scaled =
X_detect_scaled[detect_position]


建立：

profile_df = pd.DataFrame({
    "特徵": FEATURES,
    "英文名稱": [FEATURE_EN[f] for f in FEATURES],
    "原始值": [top_row[f] for f in FEATURES],
    "相對歷史標準化偏差": top_scaled
})


再建立：

profile_df["絕對偏差程度"] =
profile_df[
    "相對歷史標準化偏差"
].abs()


依：

絕對偏差程度

由高到低排序。


非常重要：

profile_df 使用：

"特徵"

不是：

"變數"


避免：

KeyError: '變數'


============================================================
STEP 17-1｜Profile 指標完整性
============================================================

Profile 必須完整包含 11 項：

Setup Overrun

Processing Overrun

Downtime Ratio

Scrap Rate

Rework Rate

Context Setup Deviation

Context Processing Deviation

Context Downtime Deviation

Context Scrap Deviation

Context Rework Deviation

Throughput Deviation


============================================================
CHART 1｜Top 15 Anomaly Score
============================================================

使用：

top_anomaly


Event Label：

"WO " + 工單ID
+ " | OP " + 工序序號
+ " | MC " + 機台ID


Title：

Top 15 MES Anomaly Events - 2026


X：

Anomaly Score (Historical Percentile)


Y：

Work Order | Operation | Machine


High Anomaly：

Red


其他：

Gold


全部圖表文字：

英文。


============================================================
CHART 2｜PCA 2D MES Anomaly Map
============================================================

固定：

pca = PCA(n_components=2)


PCA 輸入必須使用：

X_train_scaled
+
X_detect_scaled


固定：

all_scaled = np.vstack([
    X_train_scaled,
    X_detect_scaled
])


固定：

all_pca =
pca.fit_transform(all_scaled)


切回：

train_pca =
all_pca[:len(X_train_scaled)]


detect_pca =
all_pca[len(X_train_scaled):]


不可：

只對 X_detect_scaled fit PCA。


不可：

重新由原始 events 建 PCA。


不可：

建立第二組 Feature。


Historical：

Gray


2026 Normal：

Green


2026 Anomaly：

Red


Title：

MES Multi-Variable Anomaly Map - PCA


============================================================
CHART 3｜Top Anomaly Multi-Factor Deviation Profile
============================================================

Y：

profile_df["英文名稱"]


X：

profile_df[
    "相對歷史標準化偏差"
]


非常重要：

這裡的值來源是：

StandardScaler 後的
X_detect_scaled


不是重新計算 Context Z。


如果：

abs(value) >= 2

→ Red


其他：

Soft Orange


Title：

Top Anomaly - Multi-Factor Deviation Profile


============================================================
CHART 4｜Rule-Based vs Isolation Forest Matrix
============================================================

使用：

comparison_table


建立：

2 × 2 Matrix


X：

IF Normal
IF Anomaly


Y：

Rule Normal
Rule Warning


每一格顯示：

Number of Events


使用：

matplotlib


不要使用：

seaborn


============================================================
STEP 18｜AI Agent Decision Meaning
============================================================

輸出：

【後續 AI Agent 應用意義】


Isolation Forest：

辨識多變數聯合行為中相對少見的製造事件。


特別說明：

Rule Normal + IF 異常

→ 多變數隱性異常


模型提供：

Anomaly Score

Anomaly Rank

Multi-Factor Deviation Profile


AI Agent 可說：

主要偏差集中於……


不可說：

故障原因就是……


因為：

Isolation Forest 是異常偵測模型，

不是因果診斷模型。


最高優先事件：

top = top_anomaly.iloc[0]


Top 3 Feature：

profile_df.head(3)["特徵"].tolist()


============================================================
FINAL CHECK 1｜Golden Dataset Structure Validation
============================================================

若使用目前課程正式版：

YUNFA_MES.xlsx

則正確流程應自然得到：


歷史資料 2023–2025：

2,508 筆


2026 Detection：

714 筆


總 Event：

3,222 筆


如果 Event 數量不同：

不可繼續調模型。

先檢查：

Merge 是否被改動

是否額外 drop_duplicates

是否額外 dropna

是否使用錯誤年度

是否重複 Merge


============================================================
FINAL CHECK 2｜Golden Rule Threshold Validation
============================================================

只有在使用同一份課程正式版
YUNFA_MES.xlsx 時，

以下 Threshold 應自然接近：


換線超時率 95th：

0.280000


加工超時率 95th：

0.16418571428571432


停機占比 95th：

0.06081250000000011


報廢率 95th：

0.0225


重工率 95th：

0.0


產出效率偏差_Z 5th：

-1.4635803303206782


注意：

這些值只能拿來「驗證」。

不可直接把 Threshold 寫死成這些數值。


實際程式仍必須使用：

train_df[feature].quantile(q)

自行計算。


============================================================
FINAL CHECK 3｜Golden Isolation Forest Validation
============================================================

使用同一份正式資料時：

2026 Event：

714


IF Normal：

687


IF Anomaly：

27


若不是：

687 / 27

請先檢查：

11 FEATURES

FEATURE 順序

Context Z

Historical Median

StandardScaler

Isolation Forest Parameters


不得修改：

contamination

來迎合結果。


============================================================
FINAL CHECK 4｜Golden 2×2 Matrix Validation
============================================================

使用同一份正式資料時，

正確 Golden Result 應自然為：


Rule Normal + IF Normal
= 628


Rule Normal + IF Anomaly
= 1


Rule Warning + IF Normal
= 59


Rule Warning + IF Anomaly
= 26


也就是：


共同正常
= 628


多變數隱性異常
= 1


單變數規則警示
= 59


共同警示
= 26


總數：

628 + 1 + 59 + 26
= 714


如果出現：

628 / 1 / 62 / 23

或其他數字，

不得修改最終分類結果。

表示前面的 Data Pipeline
或 Isolation Forest 輸入沒有完全重現 Golden Version。


請依序檢查：

1. 2023–2025 是否 2,508 筆
2. 2026 是否 714 筆
3. events 是否 3,222 筆
4. 是否額外 drop_duplicates
5. 是否額外 dropna
6. 是否改了 Merge
7. 是否使用正確「開單日期」切年份
8. 是否正確建立報工日期
9. 停機占比是否使用：
   生產報工停機工時_hr
   ÷
   機台主檔每日可用工時
10. 六個 Context Z 是否一一對應正確
11. Primary Group 是否為：
    SKU + 工序序號
12. Fallback 是否為：
    工作中心
13. FEATURES 是否正好 11 個
14. FEATURES 順序是否一致
15. Median 是否只來自 2023–2025
16. 是否保留最後 .fillna(0)
17. StandardScaler 是否只 fit 2023–2025
18. Isolation Forest 是否固定：
    400 trees
    contamination=0.05
    random_state=42
19. Rule Threshold 是否使用 pandas quantile()
20. high 是否使用 >
21. low 是否使用 <
22. 是否把 Anomaly Score >=95
    錯當 IF 異常


============================================================
FINAL CHECK 5｜WO00830 Hidden Anomaly Validation
============================================================

不可硬編碼：

WO00830


但是使用相同正式資料與 Golden Pipeline 時，

程式應自然找到：

報工ID：

PR002727


工單ID：

WO00830


SKU：

PM-SUS-A


機台：

CNC-07


方法對照：

多變數隱性異常


其核心判定應為：

Rule Flag Count = 0

Rule判定 = Normal

IF判定 = 異常


Anomaly Score 應自然約為：

98.724


其製造指標應自然約為：


換線超時率：

0.275
= 27.5%


加工超時率：

0.1375
= 13.75%


停機占比：

0.05625
= 5.625%


報廢率：

0


重工率：

0


換線情境偏差_Z：

約 2.328559


加工情境偏差_Z：

約 1.291825


停機情境偏差_Z：

約 6.046918


報廢情境偏差_Z：

約 -0.486234


重工情境偏差_Z：

0


產出效率偏差_Z：

約 -1.226861


這些數值：

只能用於 Regression Check。

不可硬編碼到模型。

不可直接指定 PR002727 為異常。

不可直接指定 WO00830 為異常。


若自然計算結果不同：

表示前面的 Pipeline 有差異。


============================================================
FINAL｜Output Requirements
============================================================

最後請：

一次輸出完整 Python Code。

不可省略任何 STEP。

不可只輸出修改片段。

不可改變上述分析邏輯。

不可為了符合 Golden Result
直接寫死 628、1、59、26。

不可為了符合 Golden Result
直接寫死 WO00830。

Golden Result 只能用於：

Regression Validation。


程式真正的結果：

必須由 YUNFA_MES.xlsx
經由完全相同的資料處理、
Feature Engineering、
StandardScaler、
Isolation Forest
與 Rule-Based Pipeline
自然計算得到。
