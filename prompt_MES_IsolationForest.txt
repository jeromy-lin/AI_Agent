你是一位熟悉製造業 MES、Python、scikit-learn、
Isolation Forest、異常偵測與 Google Colab 的資料分析工程師。

請產生一份可以直接在 Google Colab CPU 環境執行的完整 Python 程式碼。

本次任務使用：

YUNFA_MES.xlsx

建立：

YUNFA MES－Isolation Forest 多來源、多變數製程異常偵測

請注意：

這不是重新設計一套新的 Isolation Forest 方法，
而是依照以下既定規格重現固定分析流程。

請勿自行：

- 新增 Feature
- 刪除 Feature
- 更換 Feature
- 修改年度
- 修改 Isolation Forest 參數
- 修改 Rule-Based 門檻
- 修改 Anomaly Score 計算方式
- 改變資料合併邏輯

分析單位固定為：

工單 × 工序 × 機台 × 一次實際生產報工

本案例重點是：

傳統 Rule-Based 判斷單一指標是否超標，
Isolation Forest 則分析多個製造指標的聯合行為。

程式必須能自動產生：

Rule Normal + IF 異常
→ 多變數隱性異常

並直接在結果表格的「方法對照」欄位中標註。

不要另外建立一張「多變數隱性異常」獨立表格。

最後一次輸出完整 Python Code。


============================================================
STEP 0｜Import Packages & Parameters
============================================================

使用：

import io
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt

from IPython.display import display
from google.colab import files

from sklearn.ensemble import IsolationForest
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA


固定參數：

HISTORY_YEARS = [2023, 2024, 2025]

DETECTION_YEAR = 2026

CONTAMINATION = 0.05

RANDOM_STATE = 42

TOP_N = 15


Isolation Forest 不可自行改參數。


圖表使用白色背景與柔和暖色系。

建議 Palette：

orange = #F2C6A0
green = #A8C69F
blue = #AFC8E6
purple = #D7C4E8
red = #D9534F
gold = #E8D58A
mint = #B7D7D0
gray = #D9D9D9


============================================================
STEP 1｜Upload YUNFA MES Excel
============================================================

使用：

uploaded = files.upload()

讓使用者上傳：

YUNFA_MES.xlsx


取得：

file_name = next(iter(uploaded))

並使用：

excel_bytes = io.BytesIO(uploaded[file_name])


顯示：

已載入檔案：檔案名稱


不可使用固定本機路徑。


============================================================
STEP 2｜Read MES Sheets
============================================================

使用：

pd.ExcelFile(excel_bytes)

固定讀取以下 6 張 Sheet：

1. 機台主檔
2. 工藝路由主檔
3. 生產工單
4. 生產報工
5. 機台稼動日報
6. 品質異常


DataFrame 名稱固定：

machine_df = 機台主檔

route_df = 工藝路由主檔

wo_df = 生產工單

report_df = 生產報工

equip_df = 機台稼動日報

quality_abn_df = 品質異常


先檢查：

required_sheets

如果缺少必要 Sheet：

raise ValueError

並顯示：

找不到必要 Sheet

以及：

目前 Excel 內 Sheet 名稱。


所有 Sheet 必須完整讀入。

但畫面只顯示前 5 筆。

每張表顯示：

「程式已讀取整張『Sheet 名稱』工作表；
以下僅顯示前 5 筆供資料檢視。」


使用：

df.head().style

顯示。

表格：

白底
柔和暖色
有框線
有 padding
中文字正常顯示。


============================================================
STEP 3｜Data Cleaning
============================================================

建立：

to_num(df, cols)

將指定欄位使用：

pd.to_numeric(..., errors="coerce")


日期欄位使用：

pd.to_datetime(..., errors="coerce")


【生產報工】

日期：

計畫開始時間
計畫結束時間
實際開始時間
實際結束時間


數值：

工序序號
標準換線工時_hr
實際換線工時_hr
標準加工工時_hr
實際加工工時_hr
停機工時_hr
良品數量
報廢數量
重工數量
不良率


【生產工單】

日期：

開單日期
計畫開工日期
計畫完工日期
交期
實際開工日期
實際完工日期


【機台稼動日報】

日期：

日期

轉換後請使用：

.dt.normalize()


數值：

可用工時_hr
運轉工時_hr
換線工時_hr
停機工時_hr
稼動率
品質良率
OEE
故障次數


【機台主檔】

數值：

每日可用工時
效率目標
設備年齡_年


【工藝路由主檔】

生效日期


【品質異常】

發現日期


============================================================
STEP 4｜Build Event-Level MES Dataset
============================================================

分析單位固定：

工單 × 工序 × 機台 × 一次實際生產報工


建立：

events = report_df.copy()


------------------------------------------------------------
STEP 4-1｜Merge 生產工單
------------------------------------------------------------

生產工單保留：

工單ID
產品線
開單日期
計畫開工日期
計畫完工日期
交期
計畫數量
急單
優先等級
材料齊套
品質風險
狀態


以：

工單ID

進行：

how="left"

Merge。


建立：

events["工單年度"] =
events["開單日期"].dt.year


年度切分必須使用：

生產工單的「開單日期」

不可改用：

報工日期
實際完工日期
計畫完工日期


------------------------------------------------------------
STEP 4-2｜建立報工日期
------------------------------------------------------------

必須由：

實際開始時間

建立：

events["報工日期"]


公式：

events["報工日期"] =
events["實際開始時間"].dt.normalize()


不可假設「報工日期」原本就已存在。


------------------------------------------------------------
STEP 4-3｜Merge 機台主檔
------------------------------------------------------------

機台主檔保留：

機台ID
機台類型
工作中心
能力群組
每日可用工時
效率目標
設備年齡_年
狀態


建立：

machine_merge


將機台主檔中的：

工作中心

重新命名：

機台工作中心


再以：

機台ID

how="left"

與 events Merge。


生產報工原本的「工作中心」必須保留。


------------------------------------------------------------
STEP 4-4｜Merge 機台稼動日報
------------------------------------------------------------

機台稼動日報保留：

日期
機台ID
可用工時_hr
運轉工時_hr
換線工時_hr
停機工時_hr
稼動率
品質良率
OEE
故障次數


建立：

equip_merge


重新命名：

日期
→ 報工日期


停機工時_hr
→ 設備日停機工時_hr


以：

["報工日期", "機台ID"]

進行：

how="left"

Merge。


注意：

生產報工本身的：

停機工時_hr

不可被機台稼動日報覆蓋。


機台稼動日報目前主要為 2026 資料。

因此：

OEE
稼動率
品質良率
故障次數

只用於後續設備狀態解讀。

不可加入 Isolation Forest FEATURES。


------------------------------------------------------------
STEP 4-5｜Merge 品質異常
------------------------------------------------------------

品質異常以：

["工單ID", "機台ID"]

GroupBy。


建立：

品質異常單數 =
異常單ID count


品質異常影響數量 =
受影響數量 sum


再與 events：

how="left"

Merge。


沒有紀錄者：

fillna(0)


品質異常只能作為：

Post-hoc Validation


不可作為 Isolation Forest FEATURES。


最後顯示：

生產報工原始筆數

整合後事件筆數


============================================================
STEP 5｜Context-Aware Feature Engineering
============================================================

建立：

safe_divide(a, b)


使用：

pd.to_numeric(..., errors="coerce")


分母為 0：

改為 NaN。


固定建立以下基本製造指標。


1.

換線超時率 =

(實際換線工時_hr - 標準換線工時_hr)
÷ 標準換線工時_hr


2.

加工超時率 =

(實際加工工時_hr - 標準加工工時_hr)
÷ 標準加工工時_hr


3.

停機占比 =

停機工時_hr
÷ 每日可用工時


注意：

停機占比使用：

生產報工的停機工時_hr

不是：

設備日停機工時_hr


4.

總產出數量 =

良品數量
+ 報廢數量
+ 重工數量


5.

報廢率 =

報廢數量
÷ 總產出數量

NaN：

fillna(0)


6.

重工率 =

重工數量
÷ 總產出數量

NaN：

fillna(0)


7.

良品產出率_件每時 =

良品數量
÷ 實際加工工時_hr


不可自行修改上述公式。


============================================================
STEP 6｜Historical Context Baselines
============================================================

建立：

history_mask =
events["工單年度"].isin(
    [2023, 2024, 2025]
)


建立：

detect_mask =
events["工單年度"].eq(2026)


如果沒有歷史資料：

raise ValueError


如果沒有 2026：

顯示目前可用年度，
再 raise ValueError。


建立：

add_context_z()


Context Baseline 邏輯必須固定。


Primary Group：

["SKU", "工序序號"]


Fallback Group：

["工作中心"]


首先由 2023–2025 歷史資料計算：

Primary Group Mean

Primary Group Std


若：

mean 為 NaN

或

std 為 NaN

或

abs(std) < 1e-9


才使用：

工作中心 Mean / Std


若工作中心仍無法使用，

才使用：

全部 2023–2025 歷史資料

global mean

global std


如果：

global std 為 NaN

或

abs(global std) < 1e-9

則：

global std = 1.0


情境偏差公式：

Z =

(目前值 - 歷史參考平均)
÷ 歷史參考標準差


最後：

inf
-inf

轉為 NaN，

再：

fillna(0)


建立以下 6 個欄位：

換線情境偏差_Z
加工情境偏差_Z
停機情境偏差_Z
報廢情境偏差_Z
重工情境偏差_Z
產出效率偏差_Z


============================================================
STEP 7｜Isolation Forest Features
============================================================

FEATURES 必須完全固定為：

FEATURES = [
    "換線超時率",
    "加工超時率",
    "停機占比",
    "報廢率",
    "重工率",
    "換線情境偏差_Z",
    "加工情境偏差_Z",
    "停機情境偏差_Z",
    "報廢情境偏差_Z",
    "重工情境偏差_Z",
    "產出效率偏差_Z",
]


禁止：

新增
刪除
重新排列
替換

以上 FEATURES。


建立：

FEATURE_EN


固定：

"換線超時率"
→ "Setup Overrun"

"加工超時率"
→ "Processing Overrun"

"停機占比"
→ "Downtime Ratio"

"報廢率"
→ "Scrap Rate"

"重工率"
→ "Rework Rate"

"換線情境偏差_Z"
→ "Context Setup Deviation"

"加工情境偏差_Z"
→ "Context Processing Deviation"

"停機情境偏差_Z"
→ "Context Downtime Deviation"

"報廢情境偏差_Z"
→ "Context Scrap Deviation"

"重工情境偏差_Z"
→ "Context Rework Deviation"

"產出效率偏差_Z"
→ "Throughput Deviation"


建立中文：

feature_desc


欄位：

特徵說明表可使用：

變數
商業意義


但請注意：

後面最高異常事件的 profile_df
依正確版程式使用的欄位名稱是：

「特徵」

兩者是不同 DataFrame，
不可混淆。


============================================================
STEP 7-1｜Missing Value Protection
============================================================

先處理：

events[FEATURES]


將：

np.inf
-np.inf

轉成：

np.nan


接著只使用：

2023–2025

計算：

history_medians


公式：

history_medians = (
    events.loc[history_mask, FEATURES]
    .median(numeric_only=True)
)


再：

events[FEATURES] = (
    events[FEATURES]
    .fillna(history_medians)
    .fillna(0)
)


這個：

.fillna(0)

必須保留。


目的：

如果某個 Feature 的歷史 Median 本身仍為 NaN，
最後仍要補成 0。


不可使用：

2026 Median

進行補值。


============================================================
STEP 8｜Timeline Split
============================================================

建立：

train_df =
events.loc[history_mask]
.copy()
.reset_index(drop=True)


建立：

detect_df =
events.loc[detect_mask]
.copy()
.reset_index(drop=True)


建立：

X_train =
train_df[FEATURES].copy()


X_detect =
detect_df[FEATURES].copy()


顯示：

歷史基準年度
歷史資料筆數
偵測年度
偵測資料筆數


不可：

Random Train/Test Split


============================================================
STEP 9｜Standardization
============================================================

建立：

scaler = StandardScaler()


只使用：

X_train

執行：

fit_transform()


建立：

X_train_scaled =
scaler.fit_transform(X_train)


2026：

X_detect_scaled =
scaler.transform(X_detect)


2026 不可參與：

scaler.fit()


============================================================
STEP 10｜Isolation Forest
============================================================

Isolation Forest 參數完全固定：

iso = IsolationForest(
    n_estimators=400,
    max_samples="auto",
    contamination=0.05,
    random_state=42,
    n_jobs=-1
)


使用：

iso.fit(X_train_scaled)


只使用：

2023–2025

建立模型。


建立：

train_score_raw =
-iso.score_samples(X_train_scaled)


建立：

detect_score_raw =
-iso.score_samples(X_detect_scaled)


IF 判定：

iso.predict(X_detect_scaled) == -1

→ 異常


其他：

→ 正常


建立：

detect_df["IF判定"]


============================================================
STEP 10-1｜Anomaly Score
============================================================

Anomaly Score 必須使用：

Historical Percentile


建立：

sorted_train_scores =
np.sort(train_score_raw)


2026 每筆：

Anomaly Score =

100
×
np.searchsorted(
    sorted_train_scores,
    detect score,
    side="right"
)
÷
len(sorted_train_scores)


不可改成：

Min-Max Scaling

不可改成：

decision_function


Anomaly Score 越接近：

100

代表相對歷史製造行為越少見。


建立：

Anomaly Rank


以：

Anomaly Score

由高到低。


建立：

異常層級


IF判定 == "異常"

→ High Anomaly


否則：

Anomaly Score >= 95

→ Review


其他：

→ Normal


============================================================
STEP 11｜Rule-Based Baseline
============================================================

傳統 Rule-Based 使用以下 6 個指標：

1. 換線超時率
2. 加工超時率
3. 停機占比
4. 報廢率
5. 重工率
6. 產出效率偏差_Z


門檻只使用：

train_df

也就是：

2023–2025。


固定：

換線超時率：
95th percentile

加工超時率：
95th percentile

停機占比：
95th percentile

報廢率：
95th percentile

重工率：
95th percentile

產出效率偏差_Z：
5th percentile


前五個屬於：

high


判斷：

2026 value > historical threshold


產出效率偏差_Z：

low


判斷：

2026 value < historical threshold


建立：

rule_flags


建立：

Rule Flag Count =
rule_flags.sum(axis=1)


Rule Flag Count >= 1

→ Rule判定 = Warning


否則：

→ Rule判定 = Normal


============================================================
STEP 11-1｜方法對照與多變數隱性異常標註
============================================================

這一步非常重要。

程式必須建立：

detect_df["方法對照"]


而不是只在文字說明中提到。


請使用：

np.select()


固定判斷：


Rule判定 == "Warning"
AND
IF判定 == "異常"

→ 共同警示


Rule判定 == "Normal"
AND
IF判定 == "異常"

→ 多變數隱性異常


Rule判定 == "Warning"
AND
IF判定 == "正常"

→ 單變數規則警示


其餘：

Rule判定 == "Normal"
AND
IF判定 == "正常"

→ 共同正常


注意：

「多變數隱性異常」

必須由程式模型判定結果自動產生。

不可：

指定某個工單
硬編碼某個工單ID
手動指定 WO00830


正確概念為：

Rule-Based 認為每一個單項指標都沒有超過警戒門檻，

但 Isolation Forest 根據多個指標的聯合行為，

仍判定該事件相對歷史資料不尋常。


因此：

Rule Normal + IF 異常

必須自動標註：

多變數隱性異常


不需要另外建立一張
「多變數隱性異常事件」獨立表格。

直接在後面的：

Top 15 MES 異常事件表

使用：

方法對照

欄位呈現即可。


============================================================
STEP 12｜2026 Equipment Context
============================================================

將：

OEE
稼動率
品質良率

轉為 numeric。


建立：

設備日資料可用


如果：

OEE.notna()

→ 是


否則：

→ 否


這些設備日資訊只用於：

2026 異常事件後續解讀。


============================================================
STEP 13｜Post-hoc Validation
============================================================

建立：

有品質異常紀錄


如果：

品質異常單數 > 0

→ 是


否則：

→ 否


品質異常資料不可重新加入：

Isolation Forest FEATURES。


計算：

IF判定 == 異常

的事件中，

有品質異常紀錄 == 是

所占比例。


============================================================
STEP 14｜Model Summary
============================================================

輸出：

============================================================
YUNFA MES－Isolation Forest 多來源製程異常偵測
============================================================


包含：

歷史基準年度

偵測年度

歷史基準事件數

2026 偵測事件數

Isolation Forest contamination

模型判定異常事件數

多變數隱性異常事件數

異常事件具有品質異常紀錄比例


其中：

多變數隱性異常事件數

必須由：

detect_df["方法對照"]
.eq("多變數隱性異常")
.sum()

自動計算。


顯示分析意義：

分析單位為：

工單 × 工序 × 機台 × 一次實際生產報工。


2023–2025：

建立歷史製造行為基準。


2026：

作為異常偵測對象。


Isolation Forest：

同時分析工時、停機、報廢、重工與同類製程歷史偏差。


機台稼動日報與品質異常：

僅用於結果解讀與事後驗證。


============================================================
STEP 15｜Top 15 MES Anomaly Events
============================================================

將：

detect_df

依：

Anomaly Score

以及：

Rule Flag Count

排序。


排序方式：

Anomaly Score：
descending

Rule Flag Count：
descending


取：

TOP_N = 15


建立：

top_anomaly


表格欄位：

報工ID

工單ID

SKU

工序序號

工序名稱

工作中心

機台ID

Anomaly Score

Anomaly Rank

異常層級

Rule Flag Count

Rule判定

IF判定

方法對照

換線超時率

加工超時率

停機占比

報廢率

重工率

OEE

稼動率

品質良率

故障次數

有品質異常紀錄


注意：

「方法對照」

一定要顯示在表格中。


============================================================
STEP 15-1｜Top 15 表格標註
============================================================

使用：

Pandas Styler


異常層級：

High Anomaly
→ 淡紅色


Review
→ 淡黃色


Normal
→ 淡綠色


「方法對照」欄位：

共同警示
→ 淡紅色 + 粗體


多變數隱性異常
→ 淡紫色 + 粗體


單變數規則警示
→ 淡黃色


共同正常
→ 淡綠色


特別注意：

如果相同 YUNFA_MES.xlsx 中
某筆工單自然被模型判定為：

Rule判定 = Normal

IF判定 = 異常

那麼「方法對照」欄位必須直接顯示：

多變數隱性異常


不要另外建立新的表格。

不要手動改資料。


============================================================
STEP 16｜Rule-Based vs Isolation Forest
============================================================

建立：

comparison_table = pd.crosstab(
    detect_df["Rule判定"],
    detect_df["IF判定"]
)


Rows 固定：

Normal
Warning


Columns 固定：

正常
異常


使用：

reindex()

確保即使某一類為 0，
仍保留 2 × 2 結構。


此表用於呈現：

Rule Normal + IF Normal

Rule Normal + IF Anomaly

Rule Warning + IF Normal

Rule Warning + IF Anomaly


============================================================
STEP 17｜Top Anomaly Multi-Factor Profile
============================================================

取：

top_anomaly.iloc[0]


作為最高異常事件。


取得其：

X_detect_scaled

對應位置。


建立：

profile_df


這個 DataFrame 的欄位名稱必須依正確版本固定為：

"特徵"

"英文名稱"

"原始值"

"相對歷史標準化偏差"


建立方式：

profile_df = pd.DataFrame({
    "特徵": FEATURES,
    "英文名稱": [FEATURE_EN[f] for f in FEATURES],
    "原始值": [top_row[f] for f in FEATURES],
    "相對歷史標準化偏差": top_scaled
})


再建立：

profile_df["絕對偏差程度"] =
profile_df["相對歷史標準化偏差"].abs()


依：

絕對偏差程度

由高到低排序。


注意：

這裡固定使用：

profile_df["特徵"]


不可改成：

profile_df["變數"]


避免：

KeyError: '變數'


顯示：

最高異常事件－多因素偏差輪廓


並註明：

「此輪廓描述與歷史基準的偏離程度，不代表因果關係。」


============================================================
CHART 1｜Top Anomaly Score Ranking
============================================================

使用：

top_anomaly


建立：

Horizontal Bar Chart


標題：

Top 15 MES Anomaly Events - 2026


EventLabel：

"WO " + 工單ID
+ " | OP " + 工序序號
+ " | MC " + 機台ID


例如：

WO WO00830 | OP 20 | MC CNC-07


注意：

Y 軸不要使用中文工序名稱。

所有圖表文字使用英文。


X Axis：

Anomaly Score (Historical Percentile)


Y Axis：

Work Order | Operation | Machine


High Anomaly：

Red


其他：

Gold


圖表：

白色背景。


============================================================
CHART 2｜PCA 2D MES Anomaly Map
============================================================

建立：

pca = PCA(n_components=2)


PCA 不可只對 2026 做 fit。


必須：

all_scaled = np.vstack([
    X_train_scaled,
    X_detect_scaled
])


再：

all_pca =
pca.fit_transform(all_scaled)


切回：

train_pca =
all_pca[:len(X_train_scaled)]


detect_pca =
all_pca[len(X_train_scaled):]


這樣：

2023–2025

與：

2026

才位於同一個 PCA Coordinate Space。


注意：

X_train_scaled 與 X_detect_scaled
必須來自 STEP 7 的：

history Median
+
fillna(0)
+
StandardScaler


不可重新使用未處理的原始資料。


畫面：

Historical 2023-2025
→ Gray


2026 Normal
→ Green


2026 Anomaly
→ Red


Title：

MES Multi-Variable Anomaly Map - PCA


X：

Principal Component 1


Y：

Principal Component 2


============================================================
CHART 3｜Top Anomaly Multi-Factor Deviation Profile
============================================================

使用：

profile_df


依：

相對歷史標準化偏差

排序。


Y Axis：

profile_df["英文名稱"]


不可使用中文 Feature Name。


若：

abs(value) >= 2

→ Red


其他：

→ Soft Orange


加入：

x = 0

垂直虛線。


標題：

Top Anomaly - Multi-Factor Deviation Profile


X Axis：

Standardized Deviation from Historical Baseline


Y Axis：

MES Feature


============================================================
CHART 4｜Rule-Based vs Isolation Forest Matrix
============================================================

使用：

comparison_table


建立：

2 × 2 Matrix


X Axis：

IF Normal
IF Anomaly


Y Axis：

Rule Normal
Rule Warning


每格顯示：

Number of Events


標題：

Rule-Based vs Isolation Forest


使用：

matplotlib


不可使用：

seaborn


============================================================
STEP 18｜AI Agent Decision Meaning
============================================================

輸出：

【後續 AI Agent 應用意義】


第一段：

Isolation Forest 用來辨識多變數聯合行為中
相對少見的製造事件。


尤其：

Rule Normal + IF 異常

代表：

多變數隱性異常。


第二段：

模型提供：

Anomaly Score

Anomaly Rank

Multi-Factor Deviation Profile


協助工程師判斷：

「主要偏差集中於何處」


但不可直接宣稱：

「故障原因就是……」


Isolation Forest 是：

無監督異常偵測

不是：

因果診斷模型。


============================================================
STEP 18-1｜Highest Priority Event
============================================================

若：

len(top_anomaly) > 0


取：

top = top_anomaly.iloc[0]


Top 3 偏差 Feature 必須使用：

top_dev =
profile_df.head(3)["特徵"].tolist()


注意：

一定是：

["特徵"]

不是：

["變數"]


輸出：

工單ID

工序

機台

Anomaly Score

主要偏差特徵


最後顯示：

「建議：優先檢視製程執行、停機紀錄、設備日績效與品質異常紀錄。」


============================================================
FINAL｜程式撰寫規則
============================================================

請嚴格遵守以下規格：

1. 一次提供完整 Python Code。

2. 可直接在 Google Colab 執行。

3. CPU 即可。

4. 不使用 GPU。

5. 不使用 API Key。

6. 不使用付費服務。

7. 使用：
   pandas
   numpy
   matplotlib
   scikit-learn
   openpyxl

8. 不使用 seaborn。

9. 每個 STEP 保留清楚標題。

10. 表格與說明文字使用中文。

11. matplotlib 圖表文字全部使用英文。

12. 圖表白色背景。

13. 使用柔和暖色系。

14. HISTORY_YEARS 固定：
    [2023, 2024, 2025]

15. DETECTION_YEAR 固定：
    2026

16. CONTAMINATION 固定：
    0.05

17. RANDOM_STATE 固定：
    42

18. TOP_N 固定：
    15

19. Isolation Forest 固定：
    n_estimators=400
    max_samples="auto"
    contamination=0.05
    random_state=42
    n_jobs=-1

20. FEATURES 固定 11 個，不可自行修改。

21. StandardScaler 只能使用 2023–2025 fit。

22. 2026 只能 transform。

23. Historical Median 只能由 2023–2025 計算。

24. Median 補值後必須保留：
    .fillna(0)

25. 品質異常不得作為 Isolation Forest Feature。

26. 2026 機台稼動日報不得作為 Isolation Forest Feature。

27. 年度切分必須使用「開單日期」。

28. 報工日期必須由「實際開始時間」建立。

29. Anomaly Score 必須使用歷史 score percentile。

30. Rule-Based 固定使用歷史 95 / 5 percentile。

31. 「多變數隱性異常」必須由程式自動產生。

32. 判定固定：
    Rule Normal + IF 異常
    = 多變數隱性異常

33. 不可硬編碼任何工單ID為多變數隱性異常。

34. 不可硬編碼 WO00830。

35. 若 WO00830 在相同資料與正確模型下被自然判定為：
    Rule Normal + IF 異常，
    則 Top 15 表格中的「方法對照」
    必須自動顯示：
    多變數隱性異常。

36. 不需要額外產生一張
    「多變數隱性異常事件」獨立表格。

37. 只需在原本 Top 15 表格
    的「方法對照」欄位中標註。

38. 多變數隱性異常：
    淡紫色背景 + 粗體。

39. profile_df 欄位必須使用「特徵」，
    不可改成「變數」。

40. PCA 必須將：
    X_train_scaled
    與
    X_detect_scaled
    合併後共同 fit_transform。

41. PCA 不可只 fit 2026。

42. 最後請只輸出完整可執行 Python Code，
    不要省略任何部分。
